[variables]
    NIXPACKS_NODE_VERSION = '20'

[phases.install]
    cmds = ['npm install']

[phases.build]
    cmds = ['npm run build']

[start]
    cmd = 'caddy run --config /app/Caddyfile --adapter caddyfile'
